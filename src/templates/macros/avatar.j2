{# 球員頭像：直連 MLB CDN（依最後出賽年度的最高層級決定優先 tier）→ 另一 tier 備援 → 文字縮寫 fallback（見 avatar-fallback.js） #}
{% macro avatar_img(player, player_name, lazy=False, with_ids=False) -%}
{% set cdn_primary, cdn_secondary = headshot_cdn_urls(player.mlb_id, player.latest_level_is_mlb) %}
<img
    src="{{ cdn_primary }}"
    data-cdn-src="{{ cdn_secondary }}"
    alt="{{ player_name }}"
    {%- if lazy %} loading="lazy"{% endif %}
    {%- if with_ids %} id="headshot-{{ player.mlb_id }}"{% endif %}
    class="avatar-img"
>
<div class="avatar-initials avatar-fallback"{% if with_ids %} id="initials-{{ player.mlb_id }}"{% endif %} aria-hidden="true">{{ player.name_en[:1] }}</div>
{%- endmacro %}
